<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xij.web.module.sys.user.role.UserRoleMapper">
    <select id="query" resultType="role">
        SELECT ID, NAME, CLASSIFY
        FROM SYS_ROLE R
        WHERE
        <if test='own == "0"'>not</if>
        exists(
        SELECT 1
        FROM SYS_USER_ROLE UR
        WHERE R.ID = UR.ROLE_ID
        AND UR.USER_ID = #{userId}
        and UR.STATUS = '0'
        )
        AND STATUS = '0'
        <if test='name != null and name != ""'>
            and name like concat(concat('%',#{name}),'%')
        </if>
    </select>

    <insert id="insert">
        INSERT INTO SYS_USER_ROLE(USER_ID, ROLE_ID)
        <foreach collection="roleIds" item="t" separator=" UNION ALL ">
            SELECT #{userId}, #{t} FROM DUAL
        </foreach>
    </insert>

    <delete id="delete">
        delete from SYS_USER_ROLE where ROLE_ID IN(
        <foreach collection="roleIds" item="t" separator=",">
            #{t}
        </foreach>
        )and USER_ID = #{userId}
    </delete>
</mapper>