<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xij.web.module.sys.user.right.UserRightMapper">
    <select id="query" resultType="right">
        SELECT r.ID, decode(r.TYPE,'menu',m.HREF,r.NAME) NAME, r.TYPE, decode(r.TYPE,'menu',m.NAME,r.REMARK) REMARK, r.STATUS
        FROM SYS_RIGHT r
        LEFT JOIN SYS_MENU m on r.NAME = m.ID
        WHERE
        <if test='own == "0"'>not</if>
        exists(
        SELECT 1
        FROM SYS_USER_RIGHT UR
        WHERE R.ID = UR.RIGHT_ID
        AND UR.USER_ID = #{userId}
        and UR.STATUS = '0'
        )
        and r.STATUS='0'
        <if test='name != null and name != ""'>
            AND r.NAME LIKE concat(concat('%',#{name}),'%')
        </if>
        <if test='type != null and type != ""'>
            AND r.TYPE LIKE concat(concat('%',#{type}),'%')
        </if>
    </select>

    <insert id="insert">
        INSERT INTO SYS_USER_RIGHT(USER_ID, RIGHT_ID)
        <foreach collection="rightIds" item="t" separator=" UNION ALL ">
            SELECT #{userId}, #{t} FROM DUAL
        </foreach>
    </insert>

    <delete id="delete">
        delete from SYS_USER_RIGHT where RIGHT_ID IN (
        <foreach collection="rightIds" item="t" separator=",">
            #{t}
        </foreach>
        ) and USER_ID = #{userId}
    </delete>
</mapper>